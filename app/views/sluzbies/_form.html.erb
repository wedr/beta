<%= form_for(@sluzby , :html => { :multipart => true} ) do |f| %>
  <% if @sluzby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sluzby.errors.count, "error") %> prohibited this sluzby from being saved:</h2>

      <ul>
      <% @sluzby.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <%= f.file_field :photo %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
